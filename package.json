{
  "name": "ruytha-site",
  "version": "1.0.0",
  "private": true,
  "description": "ruytha.dev — personal site with a Last.fm now-playing section",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
